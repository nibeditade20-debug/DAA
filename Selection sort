void selectionSort(int a[], int n, int i)
{
    if(i == n-1)
        return;

    int min = i;

    for(int j = i+1; j < n; j++)
    {
        if(a[j] < a[min])
            min = j;
    }

    int temp = a[i];
    a[i] = a[min];
    a[min] = temp;

    selectionSort(a, n, i+1);
}
